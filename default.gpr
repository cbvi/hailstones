project default is
   for Source_Dirs use (".");
   for Object_Dir use "build";
   for main use ("hailstones.adb");

   package Compiler is
      for Switches ("ada") use ("-gnatwa");
   end Compiler;
end default;

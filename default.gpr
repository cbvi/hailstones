project default is
   for Source_Dirs use (".");
   for Object_Dir use "build";
   for main use ("hailstones.adb");

   package Compiler is
      for Switches ("ada") use ("-gnatwa", "-O3", "-gnatn");
   end Compiler;
end default;
